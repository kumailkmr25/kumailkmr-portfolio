import type { Metadata } from "next";

export const metadata: Metadata = {
 title: "About Kumail Kmr | Web Developer from Kashmir",
};

export default function AboutLayout({ children }: { children: React.ReactNode }) {
 return children;
}

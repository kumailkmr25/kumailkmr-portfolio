import type { Metadata } from "next";

export const metadata: Metadata = {
 title: "Contact Kumail Kmr | Systems Studio",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
 return children;
}
